{
  "compilerOptions": {
    "outDir": "./dist/",
    // подсвечивает места, где не указан тип, явное указание
    "noImplicitAny": true,
    "module": "esnext",
    "target": "es5",
    "jsx": "react-jsx",
    "allowJs": true,
    // обработка как ts, так и js
    "moduleResolution": "node",
    "baseUrl": ".",
    "paths": {
      "*": ["./src/*"]
    },
    "esModuleInterop": true,
    // esModuleInterop позволяет работать с пакетами, которые используют
    // common js как с обычными пакетами с помощью import (require(), module.export) = common js
    "allowSyntheticDefaultImports": true,
    // allowSyntheticDefaultImports Если какая-либо библиотека не имеет default import,
    // ts-loader или babel-loader автоматически создают их
    // import * as React from 'react' === import React from 'react'
  },
  "ts-node": {
    "compilerOptions": {
      "module": "CommonJS"
    }
  }
}